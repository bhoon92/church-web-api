# Yakirim 기획서

> 교회 관리 웹. 작업하면서 같이 갱신해나가는 살아있는 문서.
> 결정된 건 굵게, 미정인 건 `❓` 로 표시.

---

## 0. 교회 (Church) — **멀티테넌트 root**

> **배포 모델**: 단일 인스턴스 / 단일 DB. 모든 교회가 같은 테이블 공유, `church_id`로 분기 (shared-DB multi-tenant). schema-per-tenant나 DB-per-tenant 아님.
> 이유: 초기 사용자 수가 많지 않을 예정 + 운영 단순화. 규모 커지면 그때 분리 고민.
> **모든 도메인 entity는 `church_id` FK를 가짐** (User, 헌금, 출석, 심방, FiscalYear, OfferingCategory, 계정과목 등 전부).
> 데이터 격리가 핵심 — 다른 교회 데이터는 절대 보이면 안 됨 (쿼리 레벨에서 강제, row-level security 패턴). 접속 계정의 `church_id`로 자동 필터링.

### 0.1 Church 엔티티 필드

- [ ] 교회명
- [ ] 사업자등록번호 / 고유번호 — **기부금 영수증 발급 주체**
- [ ] 대표자명 (담임목사)
- [ ] 주소 / 연락처
- [ ] 로고 (영수증·리포트용)
- [ ] 기본 회계연도 시작월 (seed용)
- [ ] 가입일 / 상태(`활성` / `체험` / `중지`)

### 0.2 멀티테넌트로 인해 영향받는 것

- **카테고리 reference table은 전부 per-church**: 헌금 종류, 계정과목, 예배 종류, 직분 등
- **회계연도(FiscalYear)도 per-church** — 교회마다 회계 시점 다름
- **User 유일성**: `(church_id, 이름)` 또는 `(church_id, 연락처)` 기준. 글로벌 unique 아님
- **권한이 2 레벨**: 시스템 admin (전체) vs 교회 내 역할 (담임/재정/부서장/일반) — 자세한 건 Cross-cutting RBAC 참조

### 0.3 결정 필요

- ~~동일인이 여러 교회에 속할 수 있는지~~ → 가능, 교차 체크 안 함 (`기술 스택 / 인증` 참조)
- super admin(SaaS 운영자) 계정 분리 방식 ❓

---

## 1. 재적 (Membership) — **데이터 모델 중심**

> 성도(User)가 도메인 모델 root (Church 안에서). 헌금 / 출석 / 심방이 모두 성도 ID에 1:N으로 연결.
> 한 명의 성도 프로필을 열면 그 사람의 모든 이력(헌금 / 출석 / 심방)이 한눈에 보여야 함.
> User는 `church_id` FK 필수 — 유일성도 `(church_id, ...)` 기준.

### 1.1 개인 정보

- [x] 이름
- [x] 연락처
- [x] 생년월일
- [x] 비고
- [ ] 세례일 / 입교일
- [ ] 등록일
- [ ] **lifecycle_stage 단일 enum** (이전 `상태`+`등록 상태` 통합):
  - `방문` / `새가족` / `정식` / `이명` / `별세` / `장기결석` / `익명`
  - **흐름**: 방문 → 새가족(등록서 작성 ~ 새가족교육 기간) → 정식 → (이명 / 별세 / 장기결석)
  - 영수증 발급은 `정식`만 가능. `익명`은 singleton User에 모든 무명 헌금 묶음(영수증 발급 불가).
  - 전체 성도 통계 쿼리는 항상 `정식`만 필터링
- [ ] **새가족 등록서 정보 (nullable, 1.1에 흡수)** — 옵션 B
  - 이전 교회 / 신앙 경력 / 등록 동기 / 직업 등 nullable text 필드
  - 새가족 교육 기간 중 특이점 / 면담 기록은 1.2 사역 기록(PastoralRecord)에 작성
- [ ] 사진 (선택)
- [ ] 직분 → 별도 history 테이블, `1.4` 참조
- [ ] 소속 (부서/사역팀/목장) → 별도 reference + 조인테이블, `1.5` 참조

### 1.x 성도에 연결되는 이력 (다른 섹션 참조)

- 헌금 이력 → `4.1 개인 헌금`
- 출석 이력 → `2. 출석부`
- 사역 기록(심방 + 새가족교육) → `1.2 PastoralRecord`
- 직분 변경 이력 → `1.4`
- 소속 변경 이력 → `1.5`

### 1.2 사역 기록 (PastoralRecord) — **심방 + 새가족교육 통합**

> 심방일지와 새가족교육 면담 노트를 동일 entity로 통합. 한 성도 프로필에서 시간순 timeline.

- [ ] `type`: 심방 / 새가족교육 / 기타 (필터·통계용)
- [ ] `recorder_user_id` (작성자, 보통 목사/전도사/팀장)
- [ ] `subject_user_id` (대상 성도)
- [ ] `date`, `location`(옵션)
- [ ] `content` (본문)
- [ ] `prayer_request` (기도제목)
- [ ] `status_note` (현재 상황)
- [ ] 예정 등록 → 알림 연계 ❓
- [ ] 권한: 작성자 + 담임 + 본인 ❓ (Cross-cutting RBAC 참조)

### 1.3 새가족 등록 (lifecycle 흐름)

교회 표준 프로세스: `방문 → 새가족 등록 작성 → 새가족 교육 → 정식 등록 → 소속 편성`

- [ ] 방문 → User 생성 (`lifecycle_stage: 방문`)
- [ ] 새가족 등록서 작성 → `lifecycle_stage: 새가족` + `1.1` 등록서 nullable 필드 채움
- [ ] 새가족 교육 (보통 3~6주) → 기간 중 특이점은 PastoralRecord(`type: 새가족교육`)로 누적
- [ ] 정식 등록 → `lifecycle_stage: 정식` + `등록일` 기록
- [ ] 소속 편성 (부서/사역팀/목장) → `1.5` 조인테이블에 추가
- [ ] **미등록 헌금 row 매칭**: 정식 등록 시 기존에 봉투로 들어온 헌금(`lifecycle_stage: 방문` User)과 매칭 ❓ (수동 / 이름·연락처 자동 추천)

### 1.4 직분 (Position)

- [ ] `Position` reference table (**per-church**, 사용자 정의)
  - seed: 성도 / 집사 / 안수집사 / 권사 / 장로 / 목사 / 부목사 / 전도사 등 (교회별 편집)
- [ ] **변경 이력 추적**: `UserPositionHistory { user_id, position_id, start_date, end_date }`
  - 현재 직분은 `end_date IS NULL` row
  - 승직·이전 직분 다 보임

### 1.5 소속 (부서 / 사역팀 / 목장) — **다 N:N**

세 개념은 한국 교회에서 다 다르게 동작 → 각각 별도 entity.

- [ ] **부서(Department)** — 연령 기반 (영아부/유치부/초등부/중고등부/청년부/장년부 등)
- [ ] **사역팀(Ministry)** — 봉사 단위 (찬양팀/새가족팀/예배안내팀/미디어팀 등). 겸직 흔함
- [ ] **목장/구역(SmallGroup)** — 생활 기반 소그룹
- 모두 **per-church 사용자 정의 reference table** (교회마다 이름 다름)
- User ↔ 각 entity는 **N:N 조인테이블** (`UserDepartment`, `UserMinistry`, `UserSmallGroup`)
- 조인테이블에 `start_date` / `end_date` 두면 소속 변경 이력도 자연스럽게 추적 ❓
- 각 entity에 리더(부서장/팀장/목자) 표현 — 조인테이블의 `role` 필드 ❓

### 결정 필요

- **lifecycle_stage 변경 이력**: 단순 현재 stage만? 변경 이력 별도 추적? ❓
- 소속 조인테이블에 기간(`start_date`/`end_date`) 둘지, 단순 현재 소속만 둘지 ❓
- 리더(부서장/팀장/목자)를 조인테이블 `role`로 둘지, 별도 표현할지 ❓

---

## 2. 출석부 (Attendance)

- [ ] 달력 연동 → `5.1` Calendar 레이어 / 외부 연동 인프라 재사용
- [ ] **예배(Service) 모델** 정의 필요 — 무엇의 출석인지
  - 주일예배 1부 / 2부
  - 수요예배
  - 새벽기도
  - 셀모임 / 구역예배
  - ❓ 위 중 어디까지 관리할지
- [ ] 출석 입력 방식: 개별 체크 / 일괄 / QR ❓
- [ ] 통계 (개인별 / 부서별 출석률)

---

## 3. 조직도 (Org Chart)

- [ ] 재적의 "소속 부서" 필드에서 derive 할지, 별도 트리 데이터 둘지 ❓
- [ ] 부서 / 구역 / 셀 계층 구조
- [ ] 부서장 / 구역장 등 role 표현
- [ ] 시각화 (트리 / 카드 뷰)

---

## 4. 재정 (Finance)

> 두 흐름을 분리: **개인 헌금(성도와 연결)** vs **운영 재정(계정과목 기반 예산/결산)**

### 4.1 개인 헌금 (성도별) — **연말정산 / 종합소득세 신고용**

- [ ] 성도(User) ↔ 헌금 내역 1:N — 누가 / 언제 / 얼마를 / 어떤 명목으로
- [ ] **모든 헌금은 `user_id` non-null** — 무명/미등록도 User row로 표현 (`1.1` lifecycle_stage 참조)
- [ ] **헌금 종류**: 사용자 정의 reference table (`OfferingCategory`, **per-church**)
  - seed: 십일조 / 감사 / 주정 / 선교 / 건축 / 절기 / 기타 (이후 교회별 편집 가능)
  - 비활성화는 soft delete(`is_active`), 과거 헌금 데이터는 유지
- [ ] **입력 UX — 두 모드 분리**
  - **빠른 입력 모드 (batch entry)**: 회계팀이 주일 후 봉투 정리할 때 사용
    - 시작 시 컨텍스트 한 번 고정: `날짜 + 예배(주일 1부 등)` → 이후 행마다 반복 안 함
    - 키보드 흐름: `성도 검색(autocomplete) → 카테고리 → 금액 → Enter → 다음 행 자동 추가`
    - **봉투 원본 이름 같이 저장**: `raw_donor_name` 필드 — 매칭 정정용
    - 매칭 안 되면 그 자리에서 **방문 단계 성도 즉석 추가** (`lifecycle_stage: 방문`)
  - **조회/관리 뷰 (노션 스타일 테이블)**: 필터·정렬·인라인 편집. 주 용도는 검토·수정·감사
- [ ] **동시 입력 지원 (여러 명 / 여러 기기)** — 한국 교회 회계팀은 보통 다수가 분산 입력
  - **헌금 세션** 개념: `(날짜 + 예배)` 단위 입력 세션, 여러 사람이 같은 세션에 붙어 자기 행 추가
  - 물리 봉투는 한 명만 들고 있어 같은 봉투 충돌 거의 없음 → 충돌 정책 단순: **last-write-wins + 변경 로그**
  - **실시간 합계 표시**: 봉투 총액 vs 입력 총합 검증용 (websocket / 짧은 polling)
  - "지금 입력 중인 사람" 표시 ❓ (있으면 좋음)
- [ ] **확정 단계 없음 — 사후 수정 자유**: 작은 교회 운영 부담 최소화. 실수 정정 가능해야 함. 대신 **변경 로그(누가/언제/뭘 바꿨는지)**로 audit
- [ ] 일괄(엑셀) 업로드 ❓ (빠른 입력으로 충분할 수도)
- [ ] **기부금 영수증 출력** (개인별 연간 합계) — 법적 필수
  - 발급 주체는 `Church.사업자등록번호` — 교회마다 다름
  - 영수증은 **세법 기준 calendar year (1/1~12/31)** — 회계연도(FiscalYear)와 별개
  - `정식` 단계만 발급 대상. 그 외(`방문`/`새가족`/`이명` 등)는 `정식`으로 promote 후 발급. `익명`은 발급 불가
- [ ] **방문 → 정식 promote 플로우** — 새가족 등록 시 기존 봉투 헌금(방문 단계 User)과 매칭 ❓ (`1.3` 참조)

### 4.2 운영 재정 (예산 / 결산)

- [ ] **`FiscalYear` 엔티티** (per-church): `name`, `start_date`, `end_date`, `is_current`
  - 교회가 자유롭게 기간 지정 (1/1~12/31, 3/1~2/28 등). 예산/결산은 `fiscal_year_id`에 묶임
- [ ] 총예산 (FiscalYear 단위)
- [ ] 입금 / 출금
- [ ] 매달 남은 금액 + **예산 대비 집행률**
- [ ] **계정과목**: 사용자 정의 reference table (per-church, 수입/지출 구분)
  - seed: 헌금수입 / 선교비 / 운영비 / 건축 / 인건비 등
- [ ] 영수증 / 증빙 첨부 ❓
- [ ] **실시간 대시보드** ⭐ — 진짜 페인 포인트
  - 회계가 정리해주기 전까지 임원·리더들이 예산/집행 현황을 못 보는 게 현장 불편
  - 회계담당자 입력 즉시 누구나(권한 있는 사람) 조회 가능해야 함
  - 헌금 입력의 "실시간 합계" 패턴과 같은 인프라 공유 (websocket / polling ❓)

### 4.2.1 부서·팀·목장별 할당 예산 (BudgetAllocation) ⭐

> 헌금/운영 재정과는 별개 트랙. 연초에 각 부서·사역팀·목장에 예산을 할당하고, 리더가 자기 단위 잔여/사용 현황을 실시간으로 조회.

- [ ] **`BudgetAllocation` 엔티티**:
  - `church_id`, `fiscal_year_id`
  - 할당 대상: `Department` / `Ministry` / `SmallGroup` 중 하나 (`target_kind` enum + `target_id`)
  - `amount` (할당 금액), `note`
  - 회계담당자가 연초에 일괄 입력 (UI는 헌금 빠른 입력처럼 batch entry 패턴 재활용 가능)
- [ ] **지출 매칭**: 운영 재정의 지출 row가 `budget_allocation_id`에 묶임 → 집계로 잔여 자동 계산
- [ ] **리더 뷰**: 부서장/팀장/목자(`1.5` 조인테이블 `role`) 가 자기 단위 예산만 조회 — `할당 / 사용 / 잔여` 카드
- [ ] **모바일 우선**: 리더가 외부에서 확인하는 케이스 많음 → 조회 화면은 반응형 필수

### 4.3 권한 / 감사

- [ ] **3단계 권한 분리**:
  - **편집(write)**: 재정담당자 + 담임
  - **전체 조회(read all)**: 임원단 / 장로 등 (예산·집행 전반 조회)
  - **단위 조회(read own scope)**: 부서장/팀장/목자 — 자기 부서·팀·목장의 BudgetAllocation만 (`1.5` `role` 기반)
- [ ] **변경 이력 로그** — 누가 언제 뭘 바꿨는지 (회계 감사용, 다른 도메인보다 강하게)
- [ ] 이중 승인 ❓ (큰 지출 건)

---

## 5. 연간행사 (Annual Calendar) + 사진 갤러리

### 5.1 Calendar 레이어 + 외부 연동 — **Google Calendar 구독 패턴**

> 행사를 단일 달력에 다 쏟아붓지 않고, **여러 calendar 레이어**로 나눈 뒤 사용자가 보고 싶은 레이어만 토글. Google Calendar 좌측 사이드바 UX 차용. **사람마다 보이는 달력이 다름**이 핵심.

- [ ] **`Calendar` 엔티티** (per-church): `name`, `kind`, `color`, `default_subscribed`
  - `kind`: `공지` / `전체` / `부서` / `사역팀` / `목장` / `개인`
  - 부서/사역팀/목장 calendar는 `1.5` reference 생성 시 자동 1:1 매핑
  - **공지사항 calendar 별도 분리** ⭐ — 광고·공지가 일반 일정과 섞여 시야 어지러워지는 걸 방지. 토글로 ON/OFF 가능, 작성 권한 별도 (`Cross-cutting RBAC` 참조)
- [ ] **개인화 뷰 (사람마다 다른 달력)**:
  - 기본 노출: `공지` + `전체` + 본인 소속(`1.5`) calendar + `개인` 자동 ON
  - 그 외 calendar는 사용자가 직접 토글로 구독·해제 (Google Calendar 좌측 패널 UX)
  - `UserCalendarSubscription { user_id, calendar_id, is_visible, color_override }` — 색상도 개인 오버라이드
  - 모바일에서는 좌측 패널 대신 상단 chip / bottom sheet 토글로 변형
- [ ] **외부 캘린더 연동 — 2단계 접근**:
  - **1단계: iCal feed (.ics) 발행** (MVP 우선)
    - calendar별 별도 feed URL → 사용자가 구글/애플/네이버/아웃룩 등 자기 캘린더 앱에 구독
    - **개인 feed URL** (본인 구독 calendar 합본 + 개인 일정) — 토큰 기반 비공개 URL, 토큰 재생성 가능
    - 단방향(읽기 전용), 구현 단순, 범용성 최고. 행사 변경 시 외부 캘린더에 자동 반영
  - **2단계: Google Calendar API 양방향** (옵션, 후속)
    - OAuth 연동한 사용자만. 앱 내 행사 생성·수정이 구글 캘린더에 push, 본인 구글 일정도 가져오기 ❓
    - 한국 일반 사용자 점유율 가장 높음. 다만 토큰 관리 부담 추가
  - **카카오 캘린더**: API 빈약 → 양방향 비추. 알림은 카톡 비즈메시지로 따로 (`Cross-cutting 알림` 참조)
- [ ] **재사용**: 출석부(`2`)의 달력 컴포넌트와 같은 인프라 — calendar 레이어 토글만 다르게 적용

### 5.2 행사 (Event)

- [ ] 주단위 / 월단위 달력
- [ ] 출석부 달력과 **컴포넌트 재사용** 가능 (같은 달력 인프라)
- [ ] **`Event` 엔티티**: `church_id`, `calendar_id` (`5.1` 레이어 — 기존 "카테고리" 대체), 이름, 일시, 장소, 설명, 주관(Department/Ministry ❓)
- [ ] 작성 권한: calendar 별로 분리 (예: 공지 calendar는 담임·부교역자만) — `Cross-cutting RBAC`

### 5.3 사진 갤러리 — **Event = 폴더 모델**

> 사용자가 행사를 만들고 그 행사 안에 사진을 업로드. 수련회·체육대회·절기예배 등 행사 단위로 사진이 묶이는 게 가장 자연스러움.

- [ ] **`Photo` 엔티티**: `event_id` (N:1), `uploader_user_id`, `storage_key`, `thumbnail_key`, `width/height`, `taken_at`(EXIF), `caption`
- [ ] **스토리지**: DB에 박지 않고 외부 object storage 필수 — **Cloudflare R2 추천** (egress 무료, 가성비)
- [ ] **업로드 UX**:
  - 드래그앤드롭 일괄 업로드 (수십~수백 장)
  - 자동 썸네일 생성 + lazy loading (모바일에서 원본 다 부르면 죽음)
  - EXIF 회전 자동 처리
  - 업로드 progress 표시
- [ ] **조회 UX (모바일 우선)**: 갤러리 그리드 / lightbox / 무한 스크롤
- [ ] zip 일괄 다운로드 ❓ (PPT용 수요)
- [ ] **권한**: 임원/리더/일반 조회 권한 — Event 기준 공개 범위 (전체 공개 / 부서원만 / 비공개) ❓
- [ ] 얼굴 인식 자동 태그 — MVP 보류

### 5.4 결정 필요

- 사진 공개 범위 단위 (Event별 vs 부서·팀별) ❓
- **교회별 스토리지 quota** — 무제한 시 비용 폭증 위험. 플랜별 quota ❓

---

## Cross-cutting (전체에 걸친 요소)

### 기술 스택 — **확정**

- **프론트**: React + Vite (PWA로 모바일 커버)
- **백엔드**: NestJS + TypeORM (현재 `apps/api`)
- **DB**: PostgreSQL
- **스토리지**: Cloudflare R2 (사진/첨부, `5.3` 참조)
- **모노레포 구조**: `apps/api` + `apps/web` 단일 repo
  - 모바일은 반응형 웹 / PWA로 1차 커버 (`모바일 정책` 참조)
  - 네이티브 앱은 필요 시점에 **별도 repo**로 분리 (React Native 가정) — 지금은 미리 분리하지 않음

### 인증 + 멀티테넌트 강제 — **확정**

- **회원가입/로그인**: Google OAuth (보급률 높고 별도 비밀번호 관리 불필요)
- **세션**: JWT에 `church_id` claim 포함, `httpOnly` cookie 저장
- **멀티테넌트 강제**: NestJS guard/interceptor가 모든 query에 `church_id` 자동 주입 (application-layer 1차). Postgres RLS 후속 보강 ❓
- **User uniqueness**: `(church_id, ...)` 기준 — **동일인 다교회 OK**, 교차 체크 안 함 (같은 사람이 A·B 교회 모두 등록돼도 별도 row)
- **역할(role)**: per-church CRUD reference table (`Role` + `UserRole` 조인) — 교회마다 직제 자유 정의
- super admin (SaaS 운영자) 분리 방식 ❓ — Google 통합 + 화이트리스트 / 별도 시스템

### 권한 (RBAC) — 초기에 잡아야 뒤가 편함

- **2 레벨 권한**:
  - 시스템 (super admin): SaaS 운영자. 모든 교회 접근. Church 생성/중지.
  - 교회 내 역할: `담임목사` / `부교역자` / `장로` / `임원` / `부서장·팀장·목자(리더)` / `재정` / `일반`
- **데이터 격리**: 교회 내 사용자는 자기 `church_id`만 조회 가능 — 쿼리 레벨 강제
- 도메인별 접근 권한 (read / write 분리):
  - **재적**: 전체 / 부서 한정 / 본인만
  - **재정 (전체)**: 편집 = 재정+담임 / 조회 = 임원·장로 추가
  - **재정 (단위 예산 `BudgetAllocation`)**: 리더는 자기 부서·팀·목장 단위만 조회 (`1.5` 조인테이블 `role` 기반 scope)
  - **사역 기록**(심방/새가족교육): 작성자 + 담임 + 본인 ❓
  - **사진 갤러리**: Event별 공개 범위 (전체 / 부서원만 / 비공개) ❓
- **역할 자체도 사용자 정의** — per-church CRUD reference table (`기술 스택 / 인증` 참조)

### 알림

- 생일
- 심방 예정
- 장기결석자 자동 감지
- 채널: 앱 내 / SMS / 카톡 ❓

### 공통 인프라

- 달력 컴포넌트 (출석부 + 연간행사 공유) — calendar 레이어 토글 + 개인 구독 + iCal feed 발행 (`5.1`)
- 검색 / 필터
- 소속 트리 / 셀렉터 (부서·사역팀·목장 — 재적 + 조직도 + 출석부 + 권한에서 공유)
- **실시간 동기화 인프라** — 헌금 입력 합계 / 재정 대시보드 / 단위 예산 잔액 등에서 공유 (websocket 또는 짧은 polling ❓)
- **오브젝트 스토리지** — 사진/첨부 파일용. Cloudflare R2 가정. 교회별 quota 정책 필요

### 디자인 시스템 / UI 톤 — **Flex(flex.team) 레퍼런스**

> "친근하고 비전문가도 부담 없는" 톤. 노션·토스보다 가볍고 따뜻한 결. 교회 일반 성도(IT 비숙련) 사용성 고려.

- **시그니처 요소**:
  - 타이포: **Pretendard** (한글 본문/제목 통합)
  - 색: 채도 낮은 파스텔 + 뉴트럴 그레이 — shadcn 기본의 "Vercel 블랙" 톤은 재정의 필요
  - 라운드: 카드 `xl` / `2xl` (12~16px), 버튼·인풋 `lg`+
  - 그림자: 옅게 (`shadow-sm` 위주), 강한 elevation 지양
  - 여백: 넉넉히. 정보 밀도보다 호흡 우선
  - 모션: 미세하고 빠른 transition (150~250ms ease-out)
- **기술 스택**:
  - Tailwind CSS + shadcn/ui (Radix 기반) + Framer Motion + Pretendard
  - shadcn은 베이스로만 두고 디자인 토큰(color / radius / shadow / spacing)을 위 톤에 맞게 재정의
- **MVP 우선순위** — 폴리시 비용 크므로 단계별:
  - 1단계: **톤 토큰 확정** (컬러 팔레트 / 타이포 스케일 / 라운드 / 그림자 / 간격) — 화면 전체 통일감의 80% 차지
  - 2단계: 모바일 조회 화면 폴리시 (`모바일 정책` 참조) — 부드러움 체감 큰 영역부터
  - 3단계: 마이크로 인터랙션 (페이지 트랜지션 / skeleton / hover 모션 / 리스트 stagger) — 후순위
- **다크모드** ❓ — Flex 자체는 라이트 우선. MVP 보류 가능

### 모바일 정책

> 현장 피드백: "**작업은 웹으로 해도 OK, 그러나 확인/열람은 모바일에서 가능해야 함**"

- **조회/열람 화면(read)**: 모바일 반응형 필수 — 사진 갤러리, 헌금 내역, 단위 예산 잔액, 재적 프로필, 사역 기록 등
- **입력 화면(write)**: 데스크탑 우선 — 헌금 batch entry, 회계 입력, 새가족 등록서 등 (모바일 fit & finish는 후순위)
- 별도 네이티브 앱은 일단 안 만듦. 잘 만든 반응형 웹 / PWA로 충분

---

## MVP 진행 순서 (제안)

0. **Church + 멀티테넌트 기반** — 모든 entity의 `church_id` FK, 데이터 격리 정책
1. **권한 + 재적** — 모든 도메인의 뼈대
2. **출석부** (예배 모델 정의 + 달력)
3. **조직도** (재적 위에 얹기)
4. **연간행사 달력** (출석부 달력 재사용)
5. **재정** (개인 헌금 → 운영 재정 순, 별도 권한 도메인이라 끝쯤에)

---

## 미해결 결정 사항 (Open Questions)

- [ ] 권한 모델 세분화 수준
- [x] ~~새가족 등록을 별도 플로우로 둘지~~ → `lifecycle_stage` 단일 enum으로 통합, 등록서 정보는 User에 nullable 흡수 (옵션 B)
- [ ] 출석 관리 범위 (주일만? 수요/새벽까지?)
- [ ] 조직도를 derive vs 독립 데이터 — `1.5` 조인테이블 기반 derive 방향으로 기울어짐
- [ ] 알림 채널 (앱 내 / 외부)
- [ ] 영수증 첨부 필요 여부
- [x] ~~회계연도 시작 시점~~ → `FiscalYear` 엔티티로 교회마다 자유 지정
- [x] ~~동일인이 여러 교회 소속 가능 여부~~ → 가능, 교차 체크 안 함
- [ ] super admin(SaaS 운영자) 분리 방식 — Google 통합 + 화이트리스트 vs 별도 시스템
- [x] ~~역할(role)도 사용자 정의로 둘지, 고정 enum으로 둘지~~ → per-church CRUD reference table
- [x] ~~비실명·익명 헌금 처리 방식~~ → User에 `lifecycle_stage` enum으로 통합 (`익명` 포함)
- [ ] 방문 → 정식 promote 시 매칭 방식 (수동 / 이름·연락처 자동 추천)
- [ ] lifecycle_stage 변경 이력 추적 여부
- [ ] 소속(부서/사역팀/목장) 조인테이블에 기간(`start/end_date`) 둘지
- [ ] 리더(부서장/팀장/목자) 표현 — 조인테이블 `role` vs 별도
- [ ] 헌금 일괄(엑셀) 업로드 필요 여부 — 빠른 입력 모드로 충분할 수도
- [x] ~~확정 단계 / 잠금~~ → 잠금 없음, 사후 수정 자유 + 변경 로그로 audit (작은 교회 부담 최소화)
- [ ] 이중 승인 필요 여부 (큰 지출 건)
- [ ] 실시간 동기화 인프라 (websocket vs 짧은 polling)
- [ ] 사진 공개 범위 단위 (Event별 vs 부서별)
- [ ] 교회별 스토리지 quota / 플랜
- [ ] BudgetAllocation 대상 hierarchy 필요 여부 (부서 전체 → 산하 팀 분배 등)
- [ ] Calendar 레이어 기본 구독 정책 — 소속(`1.5`) 기반 자동 구독 범위 (소속 부서만? 사역팀·목장도?)
- [ ] 외부 캘린더 연동 범위 — iCal feed 단방향만 vs Google Calendar API 양방향까지
- [ ] 공지사항 calendar 작성 권한 (담임·부교역자만 vs 부서장도 자기 부서 공지 가능)
- [ ] Google Calendar 양방향 시 본인 개인 일정 역방향 가져오기 여부
- [ ] 디자인 토큰 확정 — 컬러 팔레트(파스텔 계열 선택), 라운드 스케일, 그림자 스케일
- [ ] 다크모드 지원 여부 (MVP 보류 가능)
- [ ] Postgres RLS 후속 도입 여부 (application-layer guard 위에 보강)
- [ ] 호스팅 / 배포 타깃 (Vercel + Railway / Fly.io / 자체 서버 등)
- [ ] Postgres 버전 + 관리형 vs self-host
- [ ] Cloudflare R2 계정·버킷 네이밍 규칙 (church별 prefix vs 단일 버킷)
- [ ] 테스트 전략 (integration 중심 vs unit 중심, 실제 DB 사용 여부)
- [ ] 네이티브 모바일 앱 분기 시점 (어떤 시그널이 보이면 React Native repo 시작)
- [ ] 갤러리 날짜별로 검색 (ex. 매달 구분)
- [ ] 전체보기 default -> 행사보기 / 날짜순으로 보기 등으로 sorting keyword 추가
