# build output / deps (root + workspaces)
dist
node_modules
build
/tmp

# vite
apps/web/dist

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# environment variable files
.envrc
.env

# temp directory
.temp
.tmp
.pnpm-store

.claude
.vscode

# 작업용 문서 — 로컬에만 두고 커밋하지 않는다.
# 백업: ~/Documents/yakirim-notes/  (git clean -xdf 는 아래 파일들도 지우므로 주의)
HANDOFF.md
planning.md